export interface IUser {
userId: string;
firstName: string;
lastName: string;
email: string ;
password: string;
phoneNumber: string;

// CreateUser(firstName, lastName, email, phoneNumber, password): void;
}

